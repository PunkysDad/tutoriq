services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_DB: ${POSTGRES_DB:-tutoriq}
      POSTGRES_USER: ${POSTGRES_USER:-tutoriq}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-tutoriq}
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
